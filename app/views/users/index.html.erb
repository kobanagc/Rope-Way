<%= render "shared/header" %>
<div class="title-contents">
    <h2 class='service-concept'>
      理想のパートナー探し
    </h2>
</div>
<div class="main-contents">
  <% User.where.not(rope_patron_id: current_user.rope_patron_id).each do |user| %>
    <%= link_to "/users/#{user.id}", class: "user-profile-link" do %>
      <% if user.sex_id == 2 %>
      <div class="user-profiles male">
      <% else %>
      <div class="user-profiles female">
      <% end %>
        <div class="profile-image">
          <% if user.image.attached? %>
            <%= image_tag user.image, class: "icon-img" %>
          <% else %>
            <%= image_tag "default-image.png", class: "icon-img" %>
          <% end %>
        </div>
        <div class="profile-details">
          <div class="user-name">
            名前：<%= user.name %>
          </div>
          <div class="user-sex">
            性別：<%= user.sex.name %>
          </div>
          <div class="user-age">
            年齢：<%= user.age %>歳
          </div>
        </div>
      </div>
    <% end %>
  <% end %>
</div>
<%= render "shared/footer" %>
